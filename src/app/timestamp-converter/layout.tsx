import type { Metadata } from "next"; export const metadata: Metadata = { title: "Unix Timestamp Converter", description: "Convert Unix timestamps to dates and dates to Unix timestamps with this free online timestamp converter.", alternates: { canonical: "/timestamp-converter" }, }; export default function Layout({ children, }: Readonly<{ children: React.ReactNode }>) { return children; }