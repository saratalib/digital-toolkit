import type { Metadata } from "next"; export const metadata: Metadata = { title: "Free QR Code Generator Online", description: "Create QR codes online for free. Generate QR codes quickly from text, links, and other information.", alternates: { canonical: "/qr-generator" }, }; export default function Layout({ children, }: Readonly<{ children: React.ReactNode }>) { return children; }