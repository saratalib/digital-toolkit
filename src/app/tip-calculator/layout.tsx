import type { Metadata } from "next"; export const metadata: Metadata = { title: "Free Tip Calculator", description: "Calculate restaurant tips, total bills, and how much each person should pay with this free online tip calculator.", alternates: { canonical: "/tip-calculator" }, }; export default function Layout({ children, }: Readonly<{ children: React.ReactNode }>) { return children; }